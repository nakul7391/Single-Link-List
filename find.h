#ifndef FIND_H
#define FIND_H

// function to search element in the link list
status find_element(Slist_t *head, int key);

// function to seach for element in single link list recursively
status find_element_recursive(Slist_t *head, int key);

#endif
