#ifndef UTILITY_H
#define UTILITY_H

// function to print the list
status print_list(Slist_t *head);

//function to count number of nodes in a link list
int get_nodes(Slist_t *head);

// function to find number of nodes in link list recursively
int get_nodes_recursive(Slist_t *head);

#endif
